# Cargo compiled files and executables
**/target/
/dist

# These are backup files generated by rustfmt
**/*.rs.bk

# Local chain databases (default location)
**/chains/

# The cache for chain data in container
.local

# The cache for docker container dependency
/.cargo/config

.DS_Store
.idea
.vscode

# nix output result symbol link
result
.direnv/

.claude/settings.local.json

# Local wrapper scripts (hardcoded nix store paths, server-specific)
scripts/*-wrapper.sh
scripts/gen-compat-chainspec.sh
scripts/zombienet-compat-*.toml
scripts/thxnet-local-compat.json*
